const createNew = async (data) => {
  console.log("data in notifications: ", data);
  //const object = { data.content, data.time };
  //const response = await ;
  return data;
};

const exportedObject = {
  createNew,
};

export default exportedObject;
